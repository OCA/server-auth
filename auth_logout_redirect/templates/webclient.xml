<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template id="auth_logout_redirect.logout_successful" name="Logout successful">
        <t t-call="web.layout">
            <t t-set="head">
                <t t-call-assets="web.assets_frontend" t-js="false" />
            </t>
            <t t-set="body_classname" t-value="'bg-100'" />

            <div class="container py-5">
                <div
                    t-attf-class="card border-0 mx-auto bg-100 {{login_card_classes}} o_database_list"
                    style="max-width: 300px;"
                >
                    <div class="card-body">
                        <div
                            t-attf-class="text-center pb-3 border-bottom {{'mb-3' if form_small else 'mb-4'}}"
                        >
                            <img
                                t-attf-src="/web/binary/company_logo{{ '?dbname='+db if db else '' }}"
                                alt="Logo"
                                style="max-height:120px; max-width: 100%; width:auto"
                            />
                        </div>
                        <div class="oe_login_form">
                            <p>
                                You are logged out.
                            </p>
                            <div
                                t-attf-class="clearfix oe_login_buttons text-center mb-1 pt-3"
                            >
                                <a
                                    class="btn btn-primary btn-block"
                                    href="/web/login"
                                >Log in</a>
                            </div>
                        </div>
                        <div
                            class="text-center small mt-4 pt-3 border-top"
                            t-if="not disable_footer"
                        >
                            <t t-if="not disable_database_manager">
                                <a
                                    class="border-end pe-2 me-1"
                                    href="/web/database/manager"
                                >Manage Databases</a>
                            </t>
                            <a
                                href="https://www.odoo.com?utm_source=db&amp;utm_medium=auth"
                                target="_blank"
                            >Powered by <span>Odoo</span></a>
                        </div>
                    </div>
                    </div>
                </div>
        </t>
    </template>
</odoo>
