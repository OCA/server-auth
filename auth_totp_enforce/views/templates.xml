<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template id="auth_totp_setup_form" name="Two-Factor Authentication Setup">
        <t t-call="web.login_layout">
            <t t-set="disable_footer">1</t>
            <div class="oe_login_form">
                <h5 class="card-title">Set up MFA</h5>
                <div class="mb-2 mt-2 text-muted">
                    Two-factor authentication is required for your account.
                </div>
                <ul class="ps-3">
                    <li>
                        Install an authenticator app on your device
                        (Authy, Google Authenticator, Microsoft Authenticator...).
                    </li>
                    <li>Look for an "Add an account" button.</li>
                    <li>Scan the QR code below, or enter the key manually.</li>
                </ul>
                <div class="text-center mb-3">
                    <img
                        t-if="qrcode"
                        t-attf-src="data:image/png;base64,{{ qrcode }}"
                        alt="QR Code"
                        class="img-fluid"
                    />
                    <div class="mt-2">
                        <a
                            data-bs-toggle="collapse"
                            href="#collapseTotpSecret"
                            role="button"
                            aria-expanded="false"
                            aria-controls="collapseTotpSecret"
                        >Cannot scan it?</a>
                        <div class="collapse" id="collapseTotpSecret">
                            <code class="d-block mt-2" t-esc="secret" />
                        </div>
                    </div>
                </div>
                <form method="POST" action="" class="">
                    <input
                        type="hidden"
                        name="csrf_token"
                        t-att-value="request.csrf_token()"
                    />
                    <input type="hidden" name="redirect" t-att-value="redirect" />
                    <div class="mb-3">
                        <label class="fw-bold" for="totp_token">
                            Verification Code
                        </label>
                        <input
                            id="totp_token"
                            name="totp_token"
                            class="form-control my-2"
                            autocomplete="off"
                            inputmode="numeric"
                            autofocus="autofocus"
                            required="required"
                            placeholder="e.g. 123456"
                        />
                    </div>
                    <div class="text-center py-2 border-top">
                        <p class="alert alert-danger" t-if="error" role="alert">
                            <t t-esc="error" />
                        </p>
                        <div
                            t-attf-class="clearfix oe_login_buttons text-center d-grid mb-1"
                        >
                            <button type="submit" class="btn btn-primary">
                                Activate &amp; Log in
                            </button>
                        </div>
                    </div>
                </form>
                <form method="POST" action="/web/session/logout">
                    <input
                        type="hidden"
                        name="csrf_token"
                        t-att-value="request.csrf_token()"
                    />
                    <div class="w-100 text-center">
                        <button type="submit" class="btn btn-link btn-sm mb-2">
                            Cancel
                        </button>
                    </div>
                </form>
            </div>
        </t>
    </template>
</odoo>
